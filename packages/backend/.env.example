# Environment Configuration for Backend

# Server
PORT=3001
NODE_ENV=development

# CORS
CORS_ORIGIN=*

# Database (MySQL/MariaDB)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=puzzle_minigames

# Socket.io (optional configurations)
SOCKET_PING_TIMEOUT=60000
SOCKET_PING_INTERVAL=25000
